# IPM 多代理 Workshop 環境變數範本
#
# 使用方式：
#   1. 複製本檔為同目錄下的 .env（cp .env.template .env）
#   2. 依下方說明填入你自己的值
#   3. 執行 uv run python -m preflight 驗證
#
# 本檔不可包含任何實際值。.env 已列入 .gitignore，不可提交。
# 切換講師環境與學員環境時只修改本檔的值，不需改動程式碼或講義。

# ── 必要 ───────────────────────────────────────────────
# 你的 Foundry 專案端點。
# 取得：Foundry portal → 你的專案 → 首頁 → 複製「專案端點」。
FOUNDRY_PROJECT_ENDPOINT=<TODO: 貼上你的專案端點>

# 你要使用的模型部署名稱（不是模型型號）。
# 取得：Foundry portal → 你的專案 → Deployments → 複製「部署名稱」。
# 該部署必須同時支援 tool calling 與 structured outputs（前置檢查第 6 項會實測）。
MODEL_DEPLOYMENT_NAME=<TODO: 貼上你的模型部署名稱>

# ── 選用 ───────────────────────────────────────────────
# 四個 workshop agent 的名稱前置詞。一般不需修改。
# 清理腳本依這個前置詞辨識本 workshop 建立的 agent，改動後請一併調整清理範圍。
# AGENT_NAME_PREFIX=qvn-

# DevUI 的連接埠。連接埠被占用時才需修改。
# 8080 是 agent-framework-devui 的預設值（2026-07-27 實測）。
# 程式會明確傳入此值，不依賴套件預設。
# DEVUI_PORT=8080

# DevUI 的 Bearer token。**一般不需要設定**。
# 只有在 Codespaces 的連接埠轉發回 400 Bad Request 時才需要：
# 那代表轉發送來的 Host 標頭不在 loopback 允許清單內，此時必須改為繫結 0.0.0.0，
# 而繫結 0.0.0.0 就必須開啟驗證並提供 token（套件的硬性規則）。
# 設定後請在 DevUI 的設定對話框貼上同一組值。
# DEVUI_AUTH_TOKEN=<TODO: 只在需要備援路徑時自訂一組字串>
